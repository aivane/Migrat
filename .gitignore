# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — may hold real backend hosts / client IDs, never commit
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent context files — local-only, not shared via git
CLAUDE.md
AGENTS.md
context.md

# Docker deploy setup — not used for day-to-day dev (npm run dev on :5173 only)
Dockerfile
docker-compose.yml
.dockerignore
docker/
